Invoice <%= @invoice.id %>
<br/>
Amount: <%= @invoice.amount_usd %>
<br/>

<% if @invoice.expired? %>
  <b> Expired </b>
<% else %>

  To pay with bitcoin please send to: <%= @invoice.blockchain_address %>
  <br/>
  <img src="https://blockchain.info/qr?data=<%= @invoice.blockchain_address %>&size=200" />
<% end %>

<%= render partial: 'payments/index', locals: { payments: @invoice.payments } %>